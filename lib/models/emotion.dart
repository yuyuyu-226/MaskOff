import 'package:flutter/material.dart';

class Emotion {
  final String label;  final Color color; final int severity; final String category; final bool isPositive;

  Emotion(this.label, this.color, this.severity, this. category, this.isPositive);
}